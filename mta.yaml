_schema-version: 3.3.0
ID: NfaApplication
description: A simple CAP project.
version: 1.0.0
modules:
- name: NfaApplication-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: NfaDb-db
  - name: uaa_NfaApplication
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: NfaApplication-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: NfaDb-db
  parameters:
    buildpack: nodejs_buildpack
- name: NfaApplication-destination-content
  type: com.sap.application.content
  requires:
  - name: NfaApplication-destination-service
    parameters:
      content-target: true
  - name: NfaApplication_html_repo_host
    parameters:
      service-key:
        name: NfaApplication_html_repo_host-key
  - name: uaa_NfaApplication
    parameters:
      service-key:
        name: uaa_NfaApplication-key
  parameters:
    content:
      instance:
        destinations:
        - Name: NfaAppRouter_NfaApplication_html_repo_host
          ServiceInstanceName: NfaApplication-html5-app-host-service
          ServiceKeyName: NfaApplication_html_repo_host-key
          sap.cloud.service: NfaAppRouter
        - Authentication: OAuth2UserTokenExchange
          Name: NfaAppRouter_uaa_NfaApplication
          ServiceInstanceName: NfaApplication-xsuaa-service
          ServiceKeyName: uaa_NfaApplication-key
          sap.cloud.service: NfaAppRouter
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: NfaApplication-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: NfaApplication_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - nfaformui.zip
      name: nfaformui
      target-path: resources/
    - artifacts:
      - nfaapproval.zip
      name: nfaapproval
      target-path: resources/
- name: nfaformui
  type: html5
  path: app/nfaformui
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: nfaapproval
  type: html5
  path: app/nfaapproval
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: NfaDb-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: NfaApplication-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: NfaApplication-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: NfaApplication_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: NfaApplication-html5-app-host-service
    service-plan: app-host
- name: uaa_NfaApplication
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: NfaApplication-xsuaa-service
    service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
