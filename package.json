{
  "name": "NfaApplication",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap-cloud-sdk/http-client": "^4.1.1",
    "@sap-cloud-sdk/resilience": "^4.1.1",
    "@sap/cds": "^9",
    "express": "^4"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@cap-js/cds-types": "^0.13.0",
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": ">=8"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "cds": {
    "requires": {
      "destinations": true,
      "NfaBpaDest": {
        "kind": "odata",
        "credentials": {
          "destination": "NfaBpaDest"
        }
      },
      "NfaBpaDestLev2" : {
        "kind" : "odata",
        "credentials" : {
          "destination" : "NfaBpaDestLev2"
        }
      }
    }
  }
}
